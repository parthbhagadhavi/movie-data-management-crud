<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Movie</title>

  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

 <link rel="stylesheet" href="/css/editdata.css">
</head>
<body>

  <h1><i class="fas fa-edit"></i> Edit Movie</h1>

  <div class="container">
    <div class="form-section">
      <form id="editForm" method="POST" action="/updatedata?id=<%= movie._id %>" enctype="multipart/form-data">
        <label for="title">🎬 Title</label>
        <input type="text" name="title" id="title" value="<%= movie.title %>" required>

        <label for="genre">🎞️ Genre</label>
        <input type="text" name="genre" id="genre" value="<%= movie.genre %>" required>

        <label for="year">📅 Year</label>
        <input type="number" name="year" id="year" value="<%= movie.year %>" required>

        <label for="director">🎬 Director</label>
        <input type="text" name="director" id="director" value="<%= movie.director %>">

        <label for="cast">👥 Cast</label>
        <textarea name="cast" id="cast" rows="2"><%= movie.cast %></textarea>

        <label for="duration">⏱️ Duration (mins)</label>
        <input type="number" name="duration" id="duration" value="<%= movie.duration %>">

        <label for="language">🌐 Language</label>
        <input type="text" name="language" id="language" value="<%= movie.language %>">

        <label for="rating">⭐ Rating (1-10)</label>
        <input type="number" name="rating" id="rating" value="<%= movie.rating %>" min="1" max="10">

        <label for="description">📝 Description</label>
        <textarea name="description" id="description" rows="3"><%= movie.description %></textarea>

       <label for="image">🖼️ Upload New Poster</label>
<div style="display: flex; align-items: center; gap: 20px; margin-bottom: 20px;">
  <input type="file" name="image" id="image" accept="image/*" onchange="previewImage(event)">
  <img id="posterPreview" src="/<%= movie.poster %>" alt="Movie Poster" style="max-width: 120px; border-radius: 8px; border: 2px solid #00ffc8;">
</div>

        <label for="releaseDate">📆 Release Date</label>
        <input type="date" name="releaseDate" id="releaseDate" value="<%= movie.releaseDate.toISOString().split('T')[0] %>">

        <label for="trailer">🎥 Trailer Link</label>
        <input type="url" name="trailer" id="trailer" value="<%= movie.trailer %>">

        <button type="submit"><i class="fas fa-save"></i> Update Movie</button>
      </form>
    </div>
    <div class="image-section"></div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script src="/js/editdata.js"></script>
</body>
</html>
